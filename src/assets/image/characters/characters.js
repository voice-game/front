import hedgehog_idle from "./hedgehog/idle.png";
import hedgehog_walking from "./hedgehog/walking.png";

const CHARACTERS = {
  hedgehog: {
    idle: hedgehog_idle,
    walking: hedgehog_walking,
  },
};

export default CHARACTERS;
